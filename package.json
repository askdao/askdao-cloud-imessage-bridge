{
  "name": "askdao-cloud-imessage-bridge",
  "version": "0.1.0",
  "description": "iMessage bridge via BlueBubbles — translates to nanobot WebSocket protocol",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "ws": "^8.17.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
